apiVersion: v2
name: iam-service
description: A Helm chart for Kubernetes
version: 0.0.2

dependencies:
  - name: postgresql
    version: "16.0.6" # version of the postgres chart
    repository: "https://charts.bitnami.com/bitnami"
    condition: db.postgresNeedsProvision
