apiVersion: v2
name: iam-service
description: A Helm chart for Kubernetes
version: 0.0.3

dependencies:
  - name: postgresql
    version: "16.1.0" # version of the postgres chart
    repository: "https://charts.bitnami.com/bitnami"
    condition: db.postgresNeedsProvision
